{
  "name": "dsh-beacon",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Beacon — a desktop beacon for managing DeepSeek Harness plugins, skills, MCP servers, profiles, and health.",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "npm run build && electron .",
    "test": "node scripts/test-runner.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "keywords": [
    "dsh",
    "deepseek-harness",
    "electron",
    "desktop",
    "mcp",
    "skills",
    "plugins"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "yaml": "^2.9.0"
  }
}
